//문자 --> 숫자 변환 함수
export function toNumber(num1,num2) {
    // let obj = {
    //     num1 : parseInt(num1),
    //     num2 : parseInt(num2)
    // };
    // return obj;
    return {
        num1 : parseInt(num1),
        num2 : parseInt(num2)
    };
}